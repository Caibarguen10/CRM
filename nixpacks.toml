# nixpacks.toml - Configuración de build para Railway
[phases.setup]
nixPkgs = ['dotnet-sdk_8']

[phases.build]
cmds = [
  'cd CrmService',
  'dotnet restore',
  'dotnet publish -c Release -o ../out'
]

[start]
cmd = 'cd out && ./CrmService'
